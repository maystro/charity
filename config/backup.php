<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Database Backups
    |--------------------------------------------------------------------------
    |
    | إعدادات النسخ الاحتياطي لقاعدة البيانات (منطقة السوبر أدمن).
    |
    */

    // عدد النسخ الاحتياطية التي يتم الاحتفاظ بها — تُحذف الأقدم تلقائيًا.
    'keep' => (int) env('DATABASE_BACKUP_KEEP', 5),

    // المجلد الذي تُخزن فيه النسخ (داخل storage/app — مستثنى من لقطات النشر).
    'directory' => 'database-backups',

    // وقت النسخ الاحتياطي التلقائي اليومي (تنسيق 24 ساعة).
    'schedule_time' => '03:00',
];
